# 项目架构图和流程图

本文档包含项目的系统架构图、数据流程图和关键业务流程。

## 📐 系统架构图

### 整体架构

```mermaid
graph TB
    subgraph "用户交互层"
        A[用户输入] --> B[main.py<br/>主程序入口]
        B --> C[配置验证]
        C --> D[创建 QAAgent]
    end

    subgraph "代理层 (Agent Layer)"
        D --> E[QAAgent<br/>问答代理]
        E --> F[对话历史管理]
        E --> G[意图识别]
        E --> H[工具调用决策]
        E --> I[回复生成]
    end

    subgraph "LLM 层"
        I --> J[DeepSeek API<br/>大语言模型]
        J --> K[Function Calling<br/>工具调用决策]
        K --> L[工具绑定<br/>bind_tools]
    end

    subgraph "工具层 (Tools Layer)"
        L --> M[天气查询工具<br/>AmapWeatherTool]
        L --> N[信息搜索工具<br/>TavilySearchTool]
        M --> O[高德地图 API]
        N --> P[Tavily Search API]
    end

    subgraph "配置和基础设施层"
        Q[配置管理<br/>settings.py] --> E
        R[日志系统<br/>logger.py] --> E
        R --> M
        R --> N
    end

    O --> S[天气数据]
    P --> T[搜索结果]
    S --> I
    T --> I
    I --> U[用户回复]
    U --> A

    style A fill:#e1f5ff
    style E fill:#fff4e1
    style J fill:#ffe1f5
    style M fill:#e1ffe1
    style N fill:#e1ffe1
    style Q fill:#f0f0f0
    style R fill:#f0f0f0
```

### 模块依赖关系

```mermaid
graph LR
    A[main.py] --> B[qa_agent.py]
    A --> C[settings.py]
    A --> D[logger.py]

    B --> E[amap_weather_tool.py]
    B --> F[tavily_search_tool.py]
    B --> G[tool_schemas.py]
    B --> C
    B --> D

    E --> C
    F --> C
    E --> D
    F --> D

    C --> H[环境变量<br/>.env]
    D --> I[日志文件<br/>logs/]

    style A fill:#ff9999
    style B fill:#99ccff
    style C fill:#99ff99
    style D fill:#ffcc99
    style E fill:#cc99ff
    style F fill:#cc99ff
```

## 🔄 数据流程图

### 用户查询处理流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant Main as main.py
    participant Agent as QAAgent
    participant LLM as DeepSeek API
    participant Tool as 工具层
    participant API as 外部API

    User->>Main: 输入查询
    Main->>Agent: chat(user_input)
    Agent->>Agent: 更新对话历史

    alt 需要工具调用
        Agent->>LLM: 调用 LLM (bind_tools)
        LLM->>LLM: 分析意图
        LLM->>Agent: 返回工具调用请求

        alt 天气查询
            Agent->>Tool: weather_query(city)
            Tool->>API: 调用高德地图API
            API->>Tool: 返回天气数据
            Tool->>Agent: 返回格式化结果
        else 信息搜索
            Agent->>Tool: news_search(query)
            Tool->>API: 调用Tavily API
            API->>Tool: 返回搜索结果
            Tool->>Agent: 返回格式化结果
        end

        Agent->>LLM: 调用 LLM (带工具结果)
        LLM->>Agent: 生成最终回复
    else 普通对话
        Agent->>LLM: 调用 LLM (general_chain)
        LLM->>Agent: 生成回复
    end

    Agent->>Agent: 保存对话历史
    Agent->>Main: 返回结果
    Main->>User: 显示回复
```

### 工具调用决策流程

```mermaid
flowchart TD
    A[用户输入] --> B[QAAgent.chat]
    B --> C[构建消息历史]
    C --> D[调用 LLM with tools]
    D --> E{LLM 返回类型?}

    E -->|工具调用| F[解析工具调用]
    E -->|普通回复| G[直接返回回复]

    F --> H{工具类型?}
    H -->|weather_query| I[调用天气工具]
    H -->|news_search| J[调用搜索工具]

    I --> K[高德地图API]
    J --> L[Tavily API]

    K --> M[获取天气数据]
    L --> N[获取搜索结果]

    M --> O[格式化结果]
    N --> O

    O --> P[再次调用LLM]
    P --> Q[生成最终回复]
    Q --> R[返回给用户]

    G --> R

    style E fill:#ffeb3b
    style H fill:#ffeb3b
    style I fill:#4caf50
    style J fill:#4caf50
```

## 🔀 关键业务流程

### 1. 系统初始化流程

```mermaid
flowchart TD
    A[程序启动] --> B[加载环境变量]
    B --> C[初始化配置管理器]
    C --> D[验证API密钥]

    D --> E{配置验证通过?}
    E -->|否| F[显示错误信息]
    E -->|是| G[初始化日志系统]

    G --> H[创建QAAgent]
    H --> I[初始化LLM]
    I --> J[绑定工具]
    J --> K[创建对话链]
    K --> L[显示欢迎信息]
    L --> M[进入交互循环]

    F --> N[退出程序]

    style E fill:#ffeb3b
    style F fill:#f44336
    style M fill:#4caf50
```

### 2. 对话处理流程

```mermaid
flowchart TD
    A[接收用户输入] --> B{输入验证}
    B -->|空输入| A
    B -->|退出命令| C[结束会话]
    B -->|有效输入| D[记录开始时间]

    D --> E[调用 Agent.chat]
    E --> F[构建消息历史]
    F --> G[调用 LLM with tools]

    G --> H{LLM 响应类型}

    H -->|工具调用| I[执行工具]
    H -->|直接回复| J[获取回复文本]

    I --> K[解析工具参数]
    K --> L{工具类型}

    L -->|weather_query| M[调用天气API]
    L -->|news_search| N[调用搜索API]

    M --> O[获取天气数据]
    N --> P[获取搜索结果]

    O --> Q[格式化工具结果]
    P --> Q

    Q --> R[再次调用LLM]
    R --> J

    J --> S[保存对话历史]
    S --> T[计算处理时间]
    T --> U[返回结果]
    U --> V[显示给用户]
    V --> A

    C --> W[清理资源]
    W --> X[退出程序]

    style H fill:#ffeb3b
    style L fill:#ffeb3b
    style V fill:#4caf50
```

### 3. 错误处理流程

```mermaid
flowchart TD
    A[操作执行] --> B{是否成功?}
    B -->|是| C[正常返回]
    B -->|否| D[捕获异常]

    D --> E{异常类型}

    E -->|配置错误| F[显示配置错误信息]
    E -->|API错误| G[显示API错误信息]
    E -->|网络错误| H[显示网络错误信息]
    E -->|其他错误| I[显示通用错误信息]

    F --> J[记录错误日志]
    G --> J
    H --> J
    I --> J

    J --> K{是否可恢复?}
    K -->|是| L[重试或继续]
    K -->|否| M[终止操作]

    L --> A
    M --> N[返回错误结果]

    C --> O[继续流程]
    N --> P[用户提示]

    style B fill:#ffeb3b
    style E fill:#ffeb3b
    style K fill:#ffeb3b
    style C fill:#4caf50
    style M fill:#f44336
```

## 🏛️ 类图

### QAAgent 类结构

```mermaid
classDiagram
    class QAAgent {
        -session_id: str
        -conversation_history: List
        -weather_tool: AmapWeatherTool
        -search_tool: TavilySearchTool
        -llm: ChatOpenAI
        -llm_with_tools: ChatOpenAI
        -tools: List
        -general_chain: Chain
        +chat(user_input: str) Dict
        +get_conversation_history() List
        +clear_conversation() None
        +end_session() None
        -_initialize_llm() ChatOpenAI
        -_create_tools() List
        -_create_general_chain() Chain
        -_execute_weather_tool(city_name: str) str
        -_execute_search_tool(query: str) str
    }

    class AmapWeatherTool {
        -api_key: str
        -base_url: str
        +get_weather(city_name: str) Dict
        -_format_weather_data(data: Dict) str
    }

    class TavilySearchTool {
        -api_key: str
        -client: TavilyClient
        +search_news(query: str, max_results: int) Dict
        +format_search_results(data: Dict) str
        +search_and_format(query: str) str
    }

    class Settings {
        -api: APISettings
        -redis: RedisSettings
        -app: AppSettings
        +validate_all() bool
        +get_city_data_path() str
    }

    QAAgent --> AmapWeatherTool
    QAAgent --> TavilySearchTool
    QAAgent --> Settings
    AmapWeatherTool --> Settings
    TavilySearchTool --> Settings
```

## 📊 状态图

### 对话状态转换

```mermaid
stateDiagram-v2
    [*] --> 初始化: 程序启动
    初始化 --> 配置验证: 加载配置
    配置验证 --> 就绪: 验证通过
    配置验证 --> 错误: 验证失败

    就绪 --> 等待输入: 显示欢迎信息
    等待输入 --> 处理中: 接收用户输入
    处理中 --> 工具调用: 需要工具
    处理中 --> 生成回复: 普通对话

    工具调用 --> 执行工具: 解析工具参数
    执行工具 --> 获取结果: 调用外部API
    获取结果 --> 生成回复: 格式化结果

    生成回复 --> 等待输入: 显示回复
    等待输入 --> [*]: 用户退出

    错误 --> [*]: 退出程序
```

## 🔧 工具调用机制

### Function Calling 流程

```mermaid
graph LR
    A[用户输入] --> B[LLM 分析]
    B --> C{识别意图}

    C -->|天气查询| D[生成工具调用]
    C -->|信息搜索| E[生成工具调用]
    C -->|普通对话| F[生成文本回复]

    D --> G[weather_query<br/>参数: city_name]
    E --> H[news_search<br/>参数: query]

    G --> I[执行工具函数]
    H --> I

    I --> J[获取工具结果]
    J --> K[LLM 生成最终回复]

    F --> L[返回给用户]
    K --> L

    style C fill:#ffeb3b
    style I fill:#4caf50
```

## 📝 说明

### 架构设计原则

1. **分层架构**: 清晰的层次划分，便于维护和扩展
2. **单一职责**: 每个模块只负责一个功能
3. **依赖注入**: 通过配置管理统一管理依赖
4. **错误处理**: 完善的异常处理和日志记录

### 关键设计模式

1. **代理模式**: QAAgent 作为用户和工具之间的代理
2. **策略模式**: 不同工具使用不同的执行策略
3. **单例模式**: Settings 使用单例模式确保配置一致性
4. **链式调用**: 使用 LangChain LCEL 构建对话链

### 扩展点

1. **新工具添加**: 在 `tools/` 目录添加新工具类
2. **LLM 切换**: 修改 `settings.py` 中的 API 配置
3. **缓存集成**: 在工具层添加缓存逻辑
4. **持久化**: 在 Agent 层添加数据库存储

---

**注意**: 所有图表使用 Mermaid 语法，可以在支持 Mermaid 的 Markdown 查看器中查看（如 GitHub、GitLab、VS Code 等）。
